{
  "name": "workflow-ui-components",
  "version": "1.0.0",
  "description": "Shared UI components for workflow integration",
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "main": "vanilla-js/WorkflowProgressCard.js",
  "files": [
    "vanilla-js/**/*.js",
    "WorkflowProgressCard.jsx",
    "WorkflowMultiStepAccordion.jsx",
    "HumanInputCLI.jsx",
    "index.js",
    "README.md"
  ],
  "scripts": {
    "build": "vite build --config vite-vanilla.config.js",
    "build:vanilla": "vite build --config vite-vanilla.config.js",
    "build:dev": "vite build --mode development --config vite-vanilla.config.js",
    "dev": "echo 'See README.md for usage - React components are imported directly in consuming apps'",
    "preview": "echo 'See README.md for usage'",
    "test": "echo 'No tests yet'",
    "lint": "echo 'Run linter on consuming application'",
    "typecheck": "echo 'Run TypeScript check on consuming application'"
  },
  "keywords": [
    "workflow",
    "ui",
    "components",
    "react",
    "vanilla-js"
  ],
  "author": "super_starter",
  "license": "MIT",
  "note": "React components are designed to be imported directly in consuming React applications due to complex dependency management. Vanilla JS components are built as standalone UMD bundles."
}
