# User-specific settings for Bernard

[USER_PREFERENCES]
THEME = "dark_classic"
USER_RAG_ROOT = "/home/bernard/workspace/prajna_AI/prajna-stadium/llama_index/BERNARD_SPACE/Super-RAG.Bernard"  # Absolute path to this user's RAG data
RAG_TYPES = [ "RAG", "CODE_GEN", "FINANCE", "TINA_DOC" ] # Moved from system_config.toml

[GENERATE]
# User selects the method for parsing documents and creating an index.
METHOD = "LlamaParse" # Options: EasyOCR, LlamaParse, NvidiaAI, GeminiAI

[CHATBOT_AI_MODEL]
# User's personal choice for the main chatbot LLM.
SELECTED  = { PROVIDER = "nvidia",  ID = "qwen/qwen3-235b-a22b" }
MODEL_2nd = { PROVIDER = "azureAI", ID = "openai/gpt-4.1" }
MODEL_3rd = { PROVIDER = "nvidia",  ID = "igenius/colosseum_355b_instruct_16k" }
PARAMS = { temperature = 0.7, top_k = 40 }

[WORKFLOW_RAG_TYPE]
# Determines the data/storage sub-folders relative to the USER_RAG_ROOT above.
# DATA = "data.{RAG_TYPE}"
# STORAGE = "storage.{RAG_TYPE}"
agentic_rag = "CODE_GEN"
code_generator = "CODE_GEN"
deep_research  = "CODE_GEN"
document_generator = "RAG"
financial_report = "FINANCE"
human_in_the_loop = "RAG"
